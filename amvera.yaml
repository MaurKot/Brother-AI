meta:
  environment: docker
  toolchain:
    name: docker
    version: latest

build:
  dockerfile: Dockerfile

run:
  containerPort: 5000
  persistenceMount: /app/kai/state
main:
  persistenceMountSize: 1Gi
  scriptPath: ""

codex/analyze-and-fix-errors-3w25pi
