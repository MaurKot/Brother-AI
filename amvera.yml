meta:
  environment: docker
  toolchain:
    name: docker
    version: latest

build:
  dockerfile: Dockerfile

run:
  containerPort: 5000
  persistenceMount: /app/kai/state
  persistenceMountSize: 1Gi
  scriptPath: ""
